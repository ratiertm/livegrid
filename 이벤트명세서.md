# LiveView Grid 이벤트 명세서

> **버전**: v0.1.0  
> **작성일**: 2026-02-20  
> **상태**: Draft - Phoenix LiveView 이벤트 정의

---

## 📋 목차

1. [이벤트 개요](#이벤트-개요)
2. [이벤트 네이밍 규칙](#이벤트-네이밍-규칙)
3. [서버 이벤트](#서버-이벤트)
4. [클라이언트 이벤트](#클라이언트-이벤트)
5. [이벤트 페이로드](#이벤트-페이로드)
6. [에러 처리](#에러-처리)
7. [이벤트 흐름도](#이벤트-흐름도)

---

## 이벤트 개요

### 이벤트 유형

LiveView Grid는 **서버 이벤트**와 **클라이언트 이벤트**를 사용합니다.

```
Client (Browser)                    Server (LiveView)
       │                                   │
       │  phx-click="grid_sort"            │
       ├──────────────────────────────────>│
       │                                   │ handle_event("grid_sort", ...)
       │                                   │ 
       │  <diff> (WebSocket)               │
       │<──────────────────────────────────┤
       │                                   │
       │  DOM 업데이트                      │
       │                                   │
```

### 이벤트 라이프사이클

1. **발생 (Trigger)**: 사용자 액션 (클릭, 입력 등)
2. **전송 (Send)**: WebSocket으로 서버 전송
3. **처리 (Handle)**: `handle_event/3` 실행
4. **응답 (Response)**: assigns 업데이트 → Diff 계산
5. **렌더 (Render)**: 클라이언트 DOM 패치

---

## 이벤트 네이밍 규칙

### 규칙

```
grid_{action}_{target}
```

- **grid_**: 네임스페이스 (충돌 방지)
- **action**: 동작 (sort, filter, select, edit, ...)
- **target**: 대상 (column, row, cell, page, ...)

### 예시

- ✅ `grid_sort_column`
- ✅ `grid_filter_change`
- ✅ `grid_row_select`
- ❌ `sortColumn` (카멜케이스 사용 금지)
- ❌ `grid-sort` (하이픈 금지, 언더스코어 사용)

---

## 서버 이벤트

### 1. 정렬 이벤트

#### `grid_sort`

**트리거:** 컬럼 헤더 클릭  
**용도:** 데이터 정렬

**HEEx:**
```heex
<div 
  class="lv-grid__header-cell--sortable"
  phx-click="grid_sort"
  phx-value-field={column.field}
  phx-value-direction={next_sort_direction(@grid.state.sort, column.field)}
>
  <%= column.label %>
</div>
```

**핸들러:**
```elixir
def handle_event("grid_sort", params, socket) do
  %{"field" => field, "direction" => direction} = params
  
  # 정렬 로직
  sorted_data = LiveViewGrid.Sorting.sort(
    socket.assigns.grid.data,
    String.to_atom(field),
    String.to_atom(direction)
  )
  
  # assigns 업데이트
  updated_grid = %{socket.assigns.grid |
    data: sorted_data,
    state: %{socket.assigns.grid.state |
      sort: %{field: String.to_atom(field), direction: String.to_atom(direction)}
    }
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "field": "name",
  "direction": "asc" | "desc"
}
```

**응답:** assigns 업데이트 → 리렌더링

---

### 2. 필터 이벤트 (v0.2)

#### `grid_filter_change`

**트리거:** 필터 입력창 입력  
**용도:** 컬럼별 필터링

**HEEx:**
```heex
<input 
  type="text"
  class="lv-grid__filter-input"
  phx-change="grid_filter_change"
  phx-value-field={column.field}
  phx-debounce="300"
  value={get_filter_value(@grid.state.filters, column.field)}
/>
```

**핸들러:**
```elixir
def handle_event("grid_filter_change", params, socket) do
  %{"field" => field, "value" => value} = params
  field_atom = String.to_atom(field)
  
  # 필터 적용
  filtered_data = LiveViewGrid.Filtering.filter(
    socket.assigns.grid.raw_data,
    field_atom,
    value
  )
  
  # 필터 상태 업데이트
  updated_filters = Map.put(socket.assigns.grid.state.filters, field_atom, value)
  
  updated_grid = %{socket.assigns.grid |
    data: filtered_data,
    state: %{socket.assigns.grid.state | filters: updated_filters}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "field": "name",
  "value": "Alice"
}
```

**디바운싱:** 300ms (phx-debounce)

---

### 3. 페이지네이션 이벤트

#### `grid_page_change`

**트리거:** 페이지 버튼 클릭  
**용도:** 페이지 전환

**HEEx:**
```heex
<button 
  class="lv-grid__page-btn"
  phx-click="grid_page_change"
  phx-value-page={page_num}
>
  <%= page_num %>
</button>
```

**핸들러:**
```elixir
def handle_event("grid_page_change", %{"page" => page}, socket) do
  page_num = String.to_integer(page)
  grid = socket.assigns.grid
  
  # 페이지 데이터 계산
  page_data = LiveViewGrid.Pagination.paginate(
    grid.data,
    page_num,
    grid.options.page_size
  )
  
  # 페이징 상태 업데이트
  updated_pagination = %{grid.state.pagination | current_page: page_num}
  
  updated_grid = %{grid |
    state: %{grid.state | pagination: updated_pagination}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "page": "2"
}
```

---

### 4. 행 선택 이벤트

#### `grid_row_select`

**트리거:** 행 클릭  
**용도:** 행 선택/해제

**HEEx:**
```heex
<div 
  class="lv-grid__row"
  phx-click="grid_row_select"
  phx-value-row-id={row.id}
>
  ...
</div>
```

**핸들러:**
```elixir
def handle_event("grid_row_select", %{"row-id" => row_id}, socket) do
  grid = socket.assigns.grid
  id = String.to_integer(row_id)
  
  # 선택 모드에 따라 처리
  updated_selection = case grid.state.selection.mode do
    :single ->
      # 단일 선택: 기존 선택 해제 후 새로 선택
      %{grid.state.selection | selected_ids: [id]}
      
    :multiple ->
      # 다중 선택: 토글
      if id in grid.state.selection.selected_ids do
        %{grid.state.selection | 
          selected_ids: List.delete(grid.state.selection.selected_ids, id)
        }
      else
        %{grid.state.selection | 
          selected_ids: [id | grid.state.selection.selected_ids]
        }
      end
      
    :none ->
      # 선택 불가
      grid.state.selection
  end
  
  updated_grid = %{grid | 
    state: %{grid.state | selection: updated_selection}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "row-id": "3"
}
```

---

### 5. 셀 편집 이벤트 (v0.4)

#### `grid_cell_edit_start`

**트리거:** 셀 더블클릭  
**용도:** 편집 모드 진입

**HEEx:**
```heex
<div 
  class="lv-grid__cell"
  phx-dblclick="grid_cell_edit_start"
  phx-value-row-id={row.id}
  phx-value-field={column.field}
>
  ...
</div>
```

**핸들러:**
```elixir
def handle_event("grid_cell_edit_start", params, socket) do
  %{"row-id" => row_id, "field" => field} = params
  
  # 편집 상태 설정
  editing_state = %{
    row_id: String.to_integer(row_id),
    field: String.to_atom(field),
    original_value: get_cell_value(socket.assigns.grid, row_id, field)
  }
  
  updated_grid = %{socket.assigns.grid |
    state: %{socket.assigns.grid.state | editing: editing_state}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "row-id": "3",
  "field": "name"
}
```

---

#### `grid_cell_edit_submit`

**트리거:** Enter 키 또는 포커스 아웃  
**용도:** 편집 완료 및 저장

**HEEx:**
```heex
<input 
  type="text"
  phx-blur="grid_cell_edit_submit"
  phx-keydown="grid_cell_edit_keydown"
  phx-value-row-id={@grid.state.editing.row_id}
  phx-value-field={@grid.state.editing.field}
  value={@grid.state.editing.current_value}
/>
```

**핸들러:**
```elixir
def handle_event("grid_cell_edit_submit", params, socket) do
  %{"row-id" => row_id, "field" => field, "value" => new_value} = params
  
  # 데이터 업데이트
  updated_data = update_cell_value(
    socket.assigns.grid.data,
    String.to_integer(row_id),
    String.to_atom(field),
    new_value
  )
  
  # 편집 상태 초기화
  updated_grid = %{socket.assigns.grid |
    data: updated_data,
    state: %{socket.assigns.grid.state | editing: nil}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:**
```json
{
  "row-id": "3",
  "field": "name",
  "value": "Alice Updated"
}
```

---

#### `grid_cell_edit_cancel`

**트리거:** Esc 키  
**용도:** 편집 취소

**HEEx:**
```heex
<input 
  phx-keydown="grid_cell_edit_keydown"
  phx-key="Escape"
  ...
/>
```

**핸들러:**
```elixir
def handle_event("grid_cell_edit_cancel", _params, socket) do
  # 편집 상태 초기화 (변경사항 버림)
  updated_grid = %{socket.assigns.grid |
    state: %{socket.assigns.grid.state | editing: nil}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

---

### 6. 전체 선택 이벤트 (v0.2)

#### `grid_select_all`

**트리거:** 헤더 체크박스 클릭  
**용도:** 전체 선택/해제

**HEEx:**
```heex
<input 
  type="checkbox"
  phx-click="grid_select_all"
  checked={@grid.state.selection.select_all}
/>
```

**핸들러:**
```elixir
def handle_event("grid_select_all", _params, socket) do
  grid = socket.assigns.grid
  
  updated_selection = if grid.state.selection.select_all do
    # 전체 해제
    %{grid.state.selection | select_all: false, selected_ids: []}
  else
    # 전체 선택
    all_ids = Enum.map(grid.data, & &1.id)
    %{grid.state.selection | select_all: true, selected_ids: all_ids}
  end
  
  updated_grid = %{grid | 
    state: %{grid.state | selection: updated_selection}
  }
  
  {:noreply, assign(socket, grid: updated_grid)}
end
```

**페이로드:** 없음

---

## 클라이언트 이벤트

### JavaScript Hooks (선택사항)

#### Virtual Scrolling Hook (v0.3)

**파일:** `assets/js/hooks/grid_virtual_scroll.js`

```javascript
export const GridVirtualScroll = {
  mounted() {
    this.el.addEventListener('scroll', (e) => {
      const scrollTop = e.target.scrollTop
      const scrollHeight = e.target.scrollHeight
      const clientHeight = e.target.clientHeight
      
      // 80% 스크롤 시 다음 데이터 요청
      if (scrollTop + clientHeight >= scrollHeight * 0.8) {
        this.pushEvent('grid_load_more', {
          offset: this.el.dataset.currentOffset
        })
      }
    })
  }
}
```

**등록:**
```javascript
// assets/js/app.js
import { GridVirtualScroll } from './hooks/grid_virtual_scroll'

let liveSocket = new LiveSocket('/live', Socket, {
  hooks: { GridVirtualScroll }
})
```

**사용:**
```heex
<div 
  id="grid-body"
  phx-hook="GridVirtualScroll"
  data-current-offset={length(@grid.data)}
>
  ...
</div>
```

---

## 이벤트 페이로드

### 표준 페이로드 구조

```elixir
%{
  # 필수
  "event_name" => "grid_sort",
  
  # 컨텍스트
  "grid_id" => "users_grid",
  
  # 이벤트별 데이터
  "field" => "name",
  "value" => "Alice",
  
  # 타임스탬프 (선택)
  "timestamp" => System.system_time(:millisecond)
}
```

### 페이로드 검증

```elixir
defmodule LiveViewGrid.EventValidator do
  @doc """
  이벤트 페이로드 검증
  """
  def validate("grid_sort", params) do
    with {:ok, field} <- validate_field(params["field"]),
         {:ok, direction} <- validate_direction(params["direction"]) do
      {:ok, %{field: field, direction: direction}}
    end
  end
  
  defp validate_field(nil), do: {:error, :missing_field}
  defp validate_field(field) when is_binary(field) do
    {:ok, String.to_atom(field)}
  end
  defp validate_field(_), do: {:error, :invalid_field}
  
  defp validate_direction("asc"), do: {:ok, :asc}
  defp validate_direction("desc"), do: {:ok, :desc}
  defp validate_direction(_), do: {:error, :invalid_direction}
end
```

---

## 에러 처리

### 에러 유형

1. **검증 에러**: 잘못된 페이로드
2. **비즈니스 로직 에러**: 정렬 실패 등
3. **시스템 에러**: 서버 장애

### 에러 핸들링 패턴

```elixir
def handle_event("grid_sort", params, socket) do
  case safe_sort(socket.assigns.grid, params) do
    {:ok, updated_grid} ->
      {:noreply, assign(socket, grid: updated_grid)}
      
    {:error, :invalid_field} ->
      {:noreply, put_flash(socket, :error, "잘못된 필드입니다.")}
      
    {:error, :permission_denied} ->
      {:noreply, put_flash(socket, :error, "정렬 권한이 없습니다.")}
      
    {:error, reason} ->
      Logger.error("Grid sort failed: #{inspect(reason)}")
      {:noreply, put_flash(socket, :error, "정렬 중 오류가 발생했습니다.")}
  end
end

defp safe_sort(grid, params) do
  with {:ok, validated} <- EventValidator.validate("grid_sort", params),
       {:ok, sorted_data} <- Sorting.sort(grid.data, validated.field, validated.direction) do
    {:ok, %{grid | data: sorted_data}}
  end
end
```

---

## 이벤트 흐름도

### 정렬 이벤트 흐름

```
사용자 클릭
    ↓
phx-click="grid_sort"
    ↓
WebSocket 전송
    ↓
handle_event("grid_sort", params, socket)
    ↓
EventValidator.validate("grid_sort", params)
    ↓
   [OK] ────────────────────────┐
    ↓                           ↓
Sorting.sort(...)          [ERROR]
    ↓                           ↓
assigns 업데이트         put_flash(:error, ...)
    ↓                           │
Diff 계산                       │
    ↓                           │
WebSocket 응답 ←────────────────┘
    ↓
DOM 패치
    ↓
UI 업데이트
```

---

## 이벤트 목록 요약

### v0.1

| 이벤트 | 트리거 | 페이로드 | 우선순위 |
|--------|--------|---------|----------|
| `grid_sort` | 헤더 클릭 | field, direction | P0 |
| `grid_page_change` | 페이지 버튼 클릭 | page | P0 |
| `grid_row_select` | 행 클릭 | row-id | P1 |

### v0.2

| 이벤트 | 트리거 | 페이로드 | 우선순위 |
|--------|--------|---------|----------|
| `grid_filter_change` | 필터 입력 | field, value | P0 |
| `grid_select_all` | 헤더 체크박스 | - | P1 |

### v0.4

| 이벤트 | 트리거 | 페이로드 | 우선순위 |
|--------|--------|---------|----------|
| `grid_cell_edit_start` | 셀 더블클릭 | row-id, field | P0 |
| `grid_cell_edit_submit` | Enter/Blur | row-id, field, value | P0 |
| `grid_cell_edit_cancel` | Esc | - | P1 |

---

## 테스트 예시

### 이벤트 테스트 (ExUnit)

```elixir
defmodule LiveViewGridWeb.GridComponentTest do
  use LiveViewGridWeb.ConnCase
  import Phoenix.LiveViewTest
  
  test "grid_sort 이벤트 처리", %{conn: conn} do
    {:ok, view, _html} = live_isolated(conn, GridComponent,
      session: %{
        "data" => [%{name: "Bob"}, %{name: "Alice"}],
        "columns" => [%{field: :name, label: "이름", sortable: true}]
      }
    )
    
    # 정렬 이벤트 전송
    view
    |> element(".lv-grid__header-cell--sortable")
    |> render_click(%{"field" => "name", "direction" => "asc"})
    
    # 결과 확인
    html = render(view)
    [first | _] = Floki.find(html, ".lv-grid__cell")
    assert Floki.text(first) =~ "Alice"
  end
  
  test "잘못된 페이로드 에러 처리", %{conn: conn} do
    {:ok, view, _html} = live_isolated(conn, GridComponent, ...)
    
    # 잘못된 direction
    view
    |> element(".lv-grid__header-cell--sortable")
    |> render_click(%{"field" => "name", "direction" => "invalid"})
    
    # 에러 메시지 확인
    assert has_element?(view, ".alert-error", "정렬 중 오류가 발생했습니다")
  end
end
```

---

## 버전 이력

| 버전 | 날짜 | 변경사항 |
|------|------|----------|
| v0.1.0 | 2026-02-20 | 초안 작성, v0.1~v0.4 이벤트 정의 |

---

## 다음 단계

- [ ] 이벤트 핸들러 구현
- [ ] 페이로드 검증 로직 작성
- [ ] 에러 처리 테스트

🐾
