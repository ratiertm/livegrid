# LiveView Grid 화면 디자인

> **버전**: v0.1.0  
> **작성일**: 2026-02-20  
> **상태**: Draft - Wireframe/Mockup

---

## 📋 목차

1. [디자인 원칙](#디자인-원칙)
2. [컴포넌트 구조](#컴포넌트-구조)
3. [v0.1 화면 설계](#v01-화면-설계)
4. [CSS 클래스 구조](#css-클래스-구조)
5. [반응형 디자인](#반응형-디자인)
6. [다크 모드](#다크-모드)

---

## 디자인 원칙

### 1. 간결함 (Simplicity)
- 불필요한 장식 최소화
- 데이터에 집중
- 명확한 계층 구조

### 2. 일관성 (Consistency)
- 통일된 간격 (8px grid system)
- 일관된 색상 팔레트
- 표준 UI 패턴 준수

### 3. 접근성 (Accessibility)
- WCAG 2.1 AA 준수
- 키보드 네비게이션 지원
- 고대비 모드 지원

### 4. 성능 (Performance)
- 최소한의 DOM 구조
- CSS 애니메이션 활용
- 불필요한 리렌더링 방지

---

## 컴포넌트 구조

### 전체 레이아웃

```
┌─────────────────────────────────────────┐
│  Grid Container (.lv-grid)              │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ Header (.lv-grid-header)           │ │
│  │  ┌──────┬──────┬──────┬──────┐    │ │
│  │  │ Col1 │ Col2 │ Col3 │ Col4 │    │ │
│  │  └──────┴──────┴──────┴──────┘    │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ Body (.lv-grid-body)               │ │
│  │  ┌──────┬──────┬──────┬──────┐    │ │
│  │  │ Data │ Data │ Data │ Data │    │ │
│  │  ├──────┼──────┼──────┼──────┤    │ │
│  │  │ Data │ Data │ Data │ Data │    │ │
│  │  ├──────┼──────┼──────┼──────┤    │ │
│  │  │ Data │ Data │ Data │ Data │    │ │
│  │  └──────┴──────┴──────┴──────┘    │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ Footer (.lv-grid-footer)           │ │
│  │  [<] 1 2 3 ... 10 [>]   20개씩    │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## v0.1 화면 설계

### 1. 기본 그리드 (Desktop)

#### Wireframe

```
Desktop (1920x1080)

┌────────────────────────────────────────────────────────────┐
│  사용자 목록                                     [새로고침] │
├────┬───────────────┬──────────────────────┬─────────┬──────┤
│ ID │ 이름 ▲        │ 이메일                │ 가입일   │ 상태 │
├────┼───────────────┼──────────────────────┼─────────┼──────┤
│ 1  │ Alice Kim     │ alice.kim@example... │ 2024-01 │ 활성 │
│ 2  │ Bob Lee       │ bob.lee@example.com  │ 2024-02 │ 비활 │
│ 3  │ Charlie Park  │ charlie@example.com  │ 2024-03 │ 활성 │
│ 4  │ David Choi    │ david@example.com    │ 2024-04 │ 활성 │
│ 5  │ Eve Song      │ eve@example.com      │ 2024-05 │ 비활 │
│...│ ...           │ ...                  │ ...     │ ... │
│20 │ Tom Kim       │ tom@example.com      │ 2024-06 │ 활성 │
├────┴───────────────┴──────────────────────┴─────────┴──────┤
│ [<] 1 [2] 3 ... 10 [>]           총 197명  |  20개씩 보기 ▼│
└────────────────────────────────────────────────────────────┘
```

#### 치수 (px)

- 전체 너비: 1200px (max-width)
- 전체 높이: 가변 (최소 600px)
- 헤더 높이: 48px
- 행 높이: 40px
- 푸터 높이: 56px
- 좌우 패딩: 16px

---

### 2. 헤더 디자인

#### 상세 뷰

```
┌────────────────────────────────────────┐
│  Header Row (48px height)              │
├───────┬──────────────┬─────────────────┤
│  80px │    150px     │      auto       │ ← 컬럼 너비
├───────┼──────────────┼─────────────────┤
│  ID   │  이름 ▲      │  이메일          │
│       │  (sortable)  │                 │
└───────┴──────────────┴─────────────────┘
   ↑           ↑              ↑
  center     left          left
 (align)    (align)       (align)
```

#### 정렬 아이콘

**오름차순:**
```
이름 ▲
```

**내림차순:**
```
이름 ▼
```

**정렬 없음:**
```
이름 ⇅
```

---

### 3. 바디 디자인

#### 일반 행

```
┌────┬──────────────┬──────────────────┐
│ 1  │ Alice Kim    │ alice@example... │
└────┴──────────────┴──────────────────┘
  ↑         ↑               ↑
 40px    padding       text-overflow
height   8px 12px      ellipsis
```

#### 호버 상태

```
┌────┬──────────────┬──────────────────┐
│ 1  │ Alice Kim    │ alice@example... │ ← 배경: #f5f5f5
└────┴──────────────┴──────────────────┘
   ↑
 cursor: pointer
```

#### 선택 상태

```
┌────┬──────────────┬──────────────────┐
│ 1  │ Alice Kim    │ alice@example... │ ← 배경: #e3f2fd
└────┴──────────────┴──────────────────┘
   ↑
 border-left: 3px solid #2196f3
```

---

### 4. 푸터 (페이징) 디자인

#### 레이아웃

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  [<] 1 [2] 3 ... 10 [>]     총 197명  |  20개씩 보기 ▼  │
│   ↑                          ↑              ↑             │
│  버튼                      정보          드롭다운          │
│                                                           │
└──────────────────────────────────────────────────────────┘
 └─────────────────┬─────────────────┘ └──────┬───────────┘
           (flex-start)                  (flex-end)
```

#### 버튼 상태

**현재 페이지:**
```
┌───────┐
│  [2]  │ ← 배경: #2196f3, 텍스트: white
└───────┘
```

**다른 페이지:**
```
┌───────┐
│   3   │ ← 배경: transparent, 텍스트: #333
└───────┘
```

**비활성 (이전 버튼, 1페이지):**
```
┌───────┐
│  [<]  │ ← opacity: 0.3, cursor: not-allowed
└───────┘
```

---

## CSS 클래스 구조

### BEM 네이밍 컨벤션

```css
/* Block */
.lv-grid { }

/* Element */
.lv-grid__header { }
.lv-grid__body { }
.lv-grid__footer { }
.lv-grid__row { }
.lv-grid__cell { }

/* Modifier */
.lv-grid__row--selected { }
.lv-grid__row--hover { }
.lv-grid__cell--sortable { }
.lv-grid__cell--frozen { }
```

### 기본 스타일 (v0.1)

```css
/* Grid Container */
.lv-grid {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  border: 1px solid #e0e0e0;
  border-radius: 4px;
  overflow: hidden;
  background: white;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

/* Header */
.lv-grid__header {
  display: flex;
  background: #fafafa;
  border-bottom: 2px solid #e0e0e0;
  font-weight: 600;
  font-size: 14px;
  color: #333;
  position: sticky;
  top: 0;
  z-index: 10;
}

.lv-grid__header-cell {
  padding: 12px 16px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  user-select: none;
}

.lv-grid__header-cell--sortable {
  cursor: pointer;
  transition: background 0.2s;
}

.lv-grid__header-cell--sortable:hover {
  background: #eeeeee;
}

/* Body */
.lv-grid__body {
  overflow-y: auto;
  max-height: 600px;
}

.lv-grid__row {
  display: flex;
  border-bottom: 1px solid #f0f0f0;
  transition: background 0.2s;
}

.lv-grid__row:hover {
  background: #f5f5f5;
}

.lv-grid__row--selected {
  background: #e3f2fd;
  border-left: 3px solid #2196f3;
}

.lv-grid__cell {
  padding: 8px 16px;
  height: 40px;
  display: flex;
  align-items: center;
  font-size: 14px;
  color: #555;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Footer */
.lv-grid__footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  height: 56px;
  background: #fafafa;
  border-top: 1px solid #e0e0e0;
  font-size: 14px;
}

.lv-grid__pagination {
  display: flex;
  gap: 4px;
}

.lv-grid__page-btn {
  min-width: 36px;
  height: 36px;
  padding: 0 12px;
  border: 1px solid #e0e0e0;
  border-radius: 4px;
  background: white;
  cursor: pointer;
  transition: all 0.2s;
}

.lv-grid__page-btn:hover {
  background: #f5f5f5;
  border-color: #2196f3;
}

.lv-grid__page-btn--current {
  background: #2196f3;
  color: white;
  border-color: #2196f3;
}

.lv-grid__page-btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

/* Utilities */
.lv-grid__sort-icon {
  margin-left: 4px;
  font-size: 12px;
  color: #999;
}

.lv-grid__sort-icon--active {
  color: #2196f3;
}
```

---

## 반응형 디자인

### Breakpoints

```css
/* Mobile */
@media (max-width: 640px) {
  .lv-grid {
    font-size: 12px;
  }
  
  .lv-grid__header-cell,
  .lv-grid__cell {
    padding: 8px 12px;
  }
  
  /* 일부 컬럼 숨김 */
  .lv-grid__cell--hide-mobile {
    display: none;
  }
}

/* Tablet */
@media (min-width: 641px) and (max-width: 1024px) {
  .lv-grid {
    max-width: 100%;
  }
}

/* Desktop */
@media (min-width: 1025px) {
  .lv-grid {
    max-width: 1200px;
  }
}
```

### Mobile 레이아웃 (선택사항)

```
Mobile (375x667)

┌────────────────────────┐
│ 사용자 목록            │
├────────────────────────┤
│ Alice Kim          [>] │
│ alice@example.com      │
│ 2024-01-15  |  활성    │
├────────────────────────┤
│ Bob Lee            [>] │
│ bob@example.com        │
│ 2024-02-01  |  비활성  │
├────────────────────────┤
│ ...                    │
├────────────────────────┤
│ [<] 1 2 3 [>]   20개  │
└────────────────────────┘
```

---

## 다크 모드

### 색상 팔레트

#### Light Mode (기본)

```css
:root {
  --grid-bg: #ffffff;
  --grid-header-bg: #fafafa;
  --grid-border: #e0e0e0;
  --grid-hover: #f5f5f5;
  --grid-selected: #e3f2fd;
  --grid-text: #333333;
  --grid-text-secondary: #555555;
  --grid-primary: #2196f3;
}
```

#### Dark Mode

```css
[data-theme="dark"] {
  --grid-bg: #1e1e1e;
  --grid-header-bg: #2a2a2a;
  --grid-border: #3a3a3a;
  --grid-hover: #2a2a2a;
  --grid-selected: #1a3a5a;
  --grid-text: #e0e0e0;
  --grid-text-secondary: #b0b0b0;
  --grid-primary: #64b5f6;
}

.lv-grid {
  background: var(--grid-bg);
  border-color: var(--grid-border);
  color: var(--grid-text);
}

.lv-grid__header {
  background: var(--grid-header-bg);
  border-bottom-color: var(--grid-border);
}

.lv-grid__row:hover {
  background: var(--grid-hover);
}

.lv-grid__row--selected {
  background: var(--grid-selected);
  border-left-color: var(--grid-primary);
}
```

### 다크 모드 미리보기

```
┌────────────────────────────────────────┐
│  (다크 모드)                           │
│  배경: #1e1e1e                         │
├────┬──────────────┬─────────────────────┤
│ ID │ 이름 ▲       │ 이메일              │ ← 헤더: #2a2a2a
├────┼──────────────┼─────────────────────┤
│ 1  │ Alice Kim    │ alice@example...   │ ← 텍스트: #e0e0e0
│ 2  │ Bob Lee      │ bob@example.com    │
└────┴──────────────┴─────────────────────┘
```

---

## 상태별 시각적 피드백

### 1. 로딩 상태

```
┌────────────────────────────────────────┐
│           데이터 로딩 중...  ⏳        │
│         ────────────────               │
│        (스켈레톤 UI 애니메이션)        │
└────────────────────────────────────────┘
```

**CSS:**
```css
.lv-grid__skeleton {
  background: linear-gradient(
    90deg,
    #f0f0f0 25%,
    #e0e0e0 50%,
    #f0f0f0 75%
  );
  background-size: 200% 100%;
  animation: loading 1.5s infinite;
}

@keyframes loading {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

---

### 2. 빈 데이터 상태

```
┌────────────────────────────────────────┐
│ ID │ 이름 │ 이메일                     │
├────┴──────┴────────────────────────────┤
│                                        │
│           📭                           │
│      데이터가 없습니다.                 │
│                                        │
│    [ + 사용자 추가 ]                   │
│                                        │
└────────────────────────────────────────┘
```

---

### 3. 에러 상태

```
┌────────────────────────────────────────┐
│           ⚠️  오류 발생                │
│                                        │
│     데이터를 불러올 수 없습니다.        │
│                                        │
│       [ 다시 시도 ]                    │
└────────────────────────────────────────┘
```

---

## 아이콘 시스템

### 사용 아이콘 (v0.1)

| 기능 | 아이콘 | Unicode | 설명 |
|------|--------|---------|------|
| 오름차순 | ▲ | U+25B2 | 정렬 오름차순 |
| 내림차순 | ▼ | U+25BC | 정렬 내림차순 |
| 정렬 가능 | ⇅ | U+21C5 | 정렬 가능 (기본) |
| 이전 | < | U+003C | 이전 페이지 |
| 다음 | > | U+003E | 다음 페이지 |
| 로딩 | ⏳ | U+23F3 | 로딩 중 |
| 빈 데이터 | 📭 | U+1F4ED | 데이터 없음 |
| 에러 | ⚠️ | U+26A0 | 에러/경고 |

**아이콘 라이브러리 (선택사항):**
- Heroicons (Tailwind)
- Material Icons
- Lucide Icons

---

## 애니메이션

### 1. 행 선택 애니메이션

```css
.lv-grid__row {
  transition: background 0.2s ease-in-out;
}

.lv-grid__row--selected {
  animation: select-pulse 0.3s ease-out;
}

@keyframes select-pulse {
  0% { background: #e3f2fd; }
  50% { background: #bbdefb; }
  100% { background: #e3f2fd; }
}
```

---

### 2. 정렬 아이콘 회전

```css
.lv-grid__sort-icon {
  transition: transform 0.2s ease-in-out;
}

.lv-grid__sort-icon--rotate {
  transform: rotate(180deg);
}
```

---

## HEEx 템플릿 구조

```heex
<div class="lv-grid">
  <!-- Header -->
  <div class="lv-grid__header">
    <%= for column <- @grid.columns do %>
      <div 
        class={"lv-grid__header-cell #{if column.sortable, do: "lv-grid__header-cell--sortable"}"}
        style={"width: #{column.width}px"}
        phx-click={if column.sortable, do: "grid_sort", else: nil}
        phx-value-field={column.field}
      >
        <%= column.label %>
        <%= if column.sortable do %>
          <span class="lv-grid__sort-icon">
            <%= sort_icon(@grid.state.sort, column.field) %>
          </span>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Body -->
  <div class="lv-grid__body">
    <%= for row <- @grid.data do %>
      <div 
        class={"lv-grid__row #{if row.id in @grid.state.selection.selected_ids, do: "lv-grid__row--selected"}"}
        phx-click="grid_row_select"
        phx-value-row-id={row.id}
      >
        <%= for column <- @grid.columns do %>
          <div class="lv-grid__cell" style={"width: #{column.width}px"}>
            <%= Map.get(row, column.field) %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Footer -->
  <div class="lv-grid__footer">
    <div class="lv-grid__pagination">
      <button class="lv-grid__page-btn" phx-click="grid_page_change" phx-value-page={@grid.state.pagination.current_page - 1} disabled={@grid.state.pagination.current_page == 1}>
        &lt;
      </button>
      
      <%= for page <- 1..@grid.state.pagination.total_pages do %>
        <button 
          class={"lv-grid__page-btn #{if page == @grid.state.pagination.current_page, do: "lv-grid__page-btn--current"}"}
          phx-click="grid_page_change"
          phx-value-page={page}
        >
          <%= page %>
        </button>
      <% end %>
      
      <button class="lv-grid__page-btn" phx-click="grid_page_change" phx-value-page={@grid.state.pagination.current_page + 1} disabled={@grid.state.pagination.current_page == @grid.state.pagination.total_pages}>
        &gt;
      </button>
    </div>
    
    <div>
      총 <%= @grid.state.pagination.total_rows %>명
    </div>
  </div>
</div>
```

---

## 버전 이력

| 버전 | 날짜 | 변경사항 |
|------|------|----------|
| v0.1.0 | 2026-02-20 | 초안 작성, 기본 디자인 시스템 |

---

## 다음 단계

- [ ] Figma/Sketch 목업 작성 (선택사항)
- [ ] CSS 파일 작성 시작
- [ ] 프로토타입 개발

🐾
