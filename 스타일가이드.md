# LiveView Grid 스타일 가이드

> **버전**: v0.1.0  
> **작성일**: 2026-02-20  
> **상태**: Draft - CSS 규칙 및 커스터마이징

---

## 📋 목차

1. [CSS 아키텍처](#css-아키텍처)
2. [네이밍 규칙](#네이밍-규칙)
3. [색상 시스템](#색상-시스템)
4. [타이포그래피](#타이포그래피)
5. [간격 시스템](#간격-시스템)
6. [컴포넌트 스타일](#컴포넌트-스타일)
7. [테마 커스터마이징](#테마-커스터마이징)
8. [반응형 디자인](#반응형-디자인)
9. [다크 모드](#다크-모드)
10. [애니메이션](#애니메이션)

---

## CSS 아키텍처

### 방법론: BEM (Block Element Modifier)

```css
.block {}              /* 독립적인 컴포넌트 */
.block__element {}     /* 블록의 하위 요소 */
.block--modifier {}    /* 블록의 변형 */
```

### 구조

```
liveview_grid.css
├── 1. Variables        (CSS Custom Properties)
├── 2. Reset            (기본 스타일 초기화)
├── 3. Layout           (Grid 레이아웃)
├── 4. Components       (헤더, 바디, 푸터)
├── 5. Modifiers        (상태별 스타일)
├── 6. Utilities        (유틸리티 클래스)
└── 7. Themes           (테마 변형)
```

---

## 네이밍 규칙

### BEM 네이밍

#### Block (블록)
```css
.lv-grid { }
```

#### Element (요소)
```css
.lv-grid__header { }
.lv-grid__body { }
.lv-grid__footer { }
.lv-grid__row { }
.lv-grid__cell { }
.lv-grid__header-cell { }
.lv-grid__page-btn { }
.lv-grid__sort-icon { }
```

#### Modifier (수정자)
```css
/* 상태 */
.lv-grid__row--selected { }
.lv-grid__row--hover { }
.lv-grid__row--editing { }

/* 타입 */
.lv-grid__header-cell--sortable { }
.lv-grid__cell--frozen { }
.lv-grid__cell--number { }

/* 크기 */
.lv-grid--compact { }
.lv-grid--large { }
```

### 네이밍 규칙 요약

- ✅ 소문자 + 하이픈 (kebab-case)
- ✅ `lv-grid` 접두사 (네임스페이스)
- ✅ `__` (언더스코어 2개) - Element
- ✅ `--` (하이픈 2개) - Modifier
- ❌ 카멜케이스 금지 (`.lvGrid` ❌)
- ❌ 스네이크케이스 금지 (`.lv_grid` ❌)

---

## 색상 시스템

### CSS Custom Properties

```css
:root {
  /* Primary Colors */
  --lv-grid-primary: #2196f3;
  --lv-grid-primary-light: #64b5f6;
  --lv-grid-primary-dark: #1976d2;
  
  /* Gray Scale */
  --lv-grid-gray-50: #fafafa;
  --lv-grid-gray-100: #f5f5f5;
  --lv-grid-gray-200: #eeeeee;
  --lv-grid-gray-300: #e0e0e0;
  --lv-grid-gray-400: #bdbdbd;
  --lv-grid-gray-500: #9e9e9e;
  --lv-grid-gray-600: #757575;
  --lv-grid-gray-700: #616161;
  --lv-grid-gray-800: #424242;
  --lv-grid-gray-900: #212121;
  
  /* Semantic Colors */
  --lv-grid-bg: #ffffff;
  --lv-grid-text: #333333;
  --lv-grid-text-secondary: #555555;
  --lv-grid-border: #e0e0e0;
  --lv-grid-hover: #f5f5f5;
  --lv-grid-selected: #e3f2fd;
  
  /* Status Colors */
  --lv-grid-success: #4caf50;
  --lv-grid-warning: #ff9800;
  --lv-grid-error: #f44336;
  --lv-grid-info: #2196f3;
}
```

### 색상 사용 예시

```css
.lv-grid {
  background: var(--lv-grid-bg);
  color: var(--lv-grid-text);
  border: 1px solid var(--lv-grid-border);
}

.lv-grid__row:hover {
  background: var(--lv-grid-hover);
}

.lv-grid__row--selected {
  background: var(--lv-grid-selected);
  border-left: 3px solid var(--lv-grid-primary);
}
```

---

## 타이포그래피

### 폰트 스택

```css
:root {
  --lv-grid-font-family: 
    -apple-system, 
    BlinkMacSystemFont, 
    "Segoe UI", 
    Roboto, 
    "Helvetica Neue", 
    Arial, 
    "Noto Sans", 
    sans-serif;
    
  --lv-grid-font-mono: 
    "SF Mono", 
    Monaco, 
    "Cascadia Code", 
    "Roboto Mono", 
    Consolas, 
    "Courier New", 
    monospace;
}
```

### 폰트 크기

```css
:root {
  /* 기본 크기 */
  --lv-grid-font-size-xs: 12px;
  --lv-grid-font-size-sm: 13px;
  --lv-grid-font-size-md: 14px;  /* 기본 */
  --lv-grid-font-size-lg: 16px;
  --lv-grid-font-size-xl: 18px;
  
  /* 행 높이 */
  --lv-grid-line-height: 1.5;
}
```

### 폰트 두께

```css
:root {
  --lv-grid-font-weight-normal: 400;
  --lv-grid-font-weight-medium: 500;
  --lv-grid-font-weight-semibold: 600;
  --lv-grid-font-weight-bold: 700;
}
```

### 사용 예시

```css
.lv-grid {
  font-family: var(--lv-grid-font-family);
  font-size: var(--lv-grid-font-size-md);
  line-height: var(--lv-grid-line-height);
}

.lv-grid__header-cell {
  font-weight: var(--lv-grid-font-weight-semibold);
  font-size: var(--lv-grid-font-size-sm);
}

.lv-grid__cell--number {
  font-family: var(--lv-grid-font-mono);
}
```

---

## 간격 시스템

### 8px Grid System

```css
:root {
  --lv-grid-space-0: 0;
  --lv-grid-space-1: 4px;   /* 0.5 unit */
  --lv-grid-space-2: 8px;   /* 1 unit */
  --lv-grid-space-3: 12px;  /* 1.5 unit */
  --lv-grid-space-4: 16px;  /* 2 unit */
  --lv-grid-space-5: 20px;  /* 2.5 unit */
  --lv-grid-space-6: 24px;  /* 3 unit */
  --lv-grid-space-8: 32px;  /* 4 unit */
  --lv-grid-space-10: 40px; /* 5 unit */
  --lv-grid-space-12: 48px; /* 6 unit */
}
```

### 사용 예시

```css
.lv-grid__header-cell {
  padding: var(--lv-grid-space-3) var(--lv-grid-space-4);
  /* 12px 16px */
}

.lv-grid__cell {
  padding: var(--lv-grid-space-2) var(--lv-grid-space-4);
  /* 8px 16px */
}
```

---

## 컴포넌트 스타일

### Grid Container

```css
.lv-grid {
  /* 레이아웃 */
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  
  /* 스타일 */
  background: var(--lv-grid-bg);
  border: 1px solid var(--lv-grid-border);
  border-radius: 4px;
  overflow: hidden;
  
  /* 타이포그래피 */
  font-family: var(--lv-grid-font-family);
  font-size: var(--lv-grid-font-size-md);
  color: var(--lv-grid-text);
  
  /* 박스 모델 */
  box-sizing: border-box;
}

/* 전체 자식 요소 box-sizing */
.lv-grid *,
.lv-grid *::before,
.lv-grid *::after {
  box-sizing: inherit;
}
```

---

### Header

```css
.lv-grid__header {
  display: flex;
  background: var(--lv-grid-gray-50);
  border-bottom: 2px solid var(--lv-grid-border);
  position: sticky;
  top: 0;
  z-index: 10;
}

.lv-grid__header-cell {
  padding: var(--lv-grid-space-3) var(--lv-grid-space-4);
  height: 48px;
  
  display: flex;
  align-items: center;
  justify-content: space-between;
  
  font-weight: var(--lv-grid-font-weight-semibold);
  font-size: var(--lv-grid-font-size-sm);
  color: var(--lv-grid-text);
  
  user-select: none;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.lv-grid__header-cell--sortable {
  cursor: pointer;
  transition: background 0.2s ease;
}

.lv-grid__header-cell--sortable:hover {
  background: var(--lv-grid-gray-100);
}
```

---

### Body

```css
.lv-grid__body {
  overflow-y: auto;
  max-height: 600px;
}

.lv-grid__row {
  display: flex;
  border-bottom: 1px solid var(--lv-grid-gray-100);
  transition: background 0.2s ease;
}

.lv-grid__row:hover {
  background: var(--lv-grid-hover);
}

.lv-grid__row--selected {
  background: var(--lv-grid-selected);
  border-left: 3px solid var(--lv-grid-primary);
}

.lv-grid__row--editing {
  background: #fff9e6;
}

.lv-grid__cell {
  padding: var(--lv-grid-space-2) var(--lv-grid-space-4);
  height: 40px;
  
  display: flex;
  align-items: center;
  
  font-size: var(--lv-grid-font-size-md);
  color: var(--lv-grid-text-secondary);
  
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 정렬 */
.lv-grid__cell--align-left {
  justify-content: flex-start;
}

.lv-grid__cell--align-center {
  justify-content: center;
}

.lv-grid__cell--align-right {
  justify-content: flex-end;
}

/* 타입별 스타일 */
.lv-grid__cell--number {
  font-family: var(--lv-grid-font-mono);
  justify-content: flex-end;
}

.lv-grid__cell--frozen {
  position: sticky;
  left: 0;
  background: var(--lv-grid-bg);
  z-index: 5;
}
```

---

### Footer

```css
.lv-grid__footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  
  padding: var(--lv-grid-space-3) var(--lv-grid-space-4);
  height: 56px;
  
  background: var(--lv-grid-gray-50);
  border-top: 1px solid var(--lv-grid-border);
  
  font-size: var(--lv-grid-font-size-sm);
}

.lv-grid__pagination {
  display: flex;
  gap: var(--lv-grid-space-1);
}

.lv-grid__page-btn {
  min-width: 36px;
  height: 36px;
  padding: 0 var(--lv-grid-space-3);
  
  border: 1px solid var(--lv-grid-border);
  border-radius: 4px;
  background: var(--lv-grid-bg);
  
  font-size: var(--lv-grid-font-size-sm);
  color: var(--lv-grid-text);
  
  cursor: pointer;
  transition: all 0.2s ease;
}

.lv-grid__page-btn:hover {
  background: var(--lv-grid-hover);
  border-color: var(--lv-grid-primary);
}

.lv-grid__page-btn--current {
  background: var(--lv-grid-primary);
  color: white;
  border-color: var(--lv-grid-primary);
}

.lv-grid__page-btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```

---

## 테마 커스터마이징

### 기본 사용법

```css
/* 사용자 CSS에서 변수 재정의 */
:root {
  --lv-grid-primary: #9c27b0;        /* 보라색 테마 */
  --lv-grid-selected: #f3e5f5;
}
```

### 테마 클래스

```css
/* 컴팩트 테마 */
.lv-grid--compact {
  --lv-grid-font-size-md: 13px;
  --lv-grid-space-2: 6px;
  --lv-grid-space-3: 10px;
}

.lv-grid--compact .lv-grid__header-cell {
  height: 40px;
}

.lv-grid--compact .lv-grid__cell {
  height: 32px;
}

/* 큰 테마 */
.lv-grid--large {
  --lv-grid-font-size-md: 16px;
  --lv-grid-space-2: 12px;
  --lv-grid-space-3: 16px;
}

.lv-grid--large .lv-grid__header-cell {
  height: 56px;
}

.lv-grid--large .lv-grid__cell {
  height: 48px;
}
```

### 사용 예시

```heex
<!-- 컴팩트 -->
<div class="lv-grid lv-grid--compact">
  ...
</div>

<!-- 큰 크기 -->
<div class="lv-grid lv-grid--large">
  ...
</div>
```

---

## 반응형 디자인

### Breakpoints

```css
:root {
  --lv-grid-breakpoint-sm: 640px;   /* Mobile */
  --lv-grid-breakpoint-md: 768px;   /* Tablet */
  --lv-grid-breakpoint-lg: 1024px;  /* Desktop */
  --lv-grid-breakpoint-xl: 1280px;  /* Wide */
}
```

### Media Queries

```css
/* Mobile (< 640px) */
@media (max-width: 639px) {
  .lv-grid {
    font-size: var(--lv-grid-font-size-sm);
  }
  
  .lv-grid__header-cell,
  .lv-grid__cell {
    padding: var(--lv-grid-space-2) var(--lv-grid-space-3);
  }
  
  /* 일부 컬럼 숨김 */
  .lv-grid__cell--hide-mobile {
    display: none;
  }
}

/* Tablet (640px ~ 1024px) */
@media (min-width: 640px) and (max-width: 1023px) {
  .lv-grid {
    max-width: 100%;
  }
}

/* Desktop (> 1024px) */
@media (min-width: 1024px) {
  .lv-grid {
    max-width: 1200px;
  }
}
```

---

## 다크 모드

### 다크 테마 변수

```css
[data-theme="dark"] {
  /* Background */
  --lv-grid-bg: #1e1e1e;
  --lv-grid-gray-50: #2a2a2a;
  --lv-grid-gray-100: #333333;
  
  /* Border */
  --lv-grid-border: #3a3a3a;
  
  /* Text */
  --lv-grid-text: #e0e0e0;
  --lv-grid-text-secondary: #b0b0b0;
  
  /* Interactive */
  --lv-grid-hover: #2a2a2a;
  --lv-grid-selected: #1a3a5a;
  
  /* Primary */
  --lv-grid-primary: #64b5f6;
  --lv-grid-primary-light: #90caf9;
}
```

### 적용 방법

```html
<!-- 수동 토글 -->
<html data-theme="dark">
  ...
</html>

<!-- 시스템 설정 따르기 -->
<script>
  if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
    document.documentElement.dataset.theme = 'dark'
  }
</script>
```

---

## 애니메이션

### Transition

```css
:root {
  --lv-grid-transition-fast: 0.1s ease;
  --lv-grid-transition-normal: 0.2s ease;
  --lv-grid-transition-slow: 0.3s ease;
}

.lv-grid__row {
  transition: background var(--lv-grid-transition-normal);
}

.lv-grid__page-btn {
  transition: all var(--lv-grid-transition-normal);
}
```

### Keyframe Animations

```css
/* 로딩 스켈레톤 */
@keyframes lv-grid-skeleton-pulse {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

.lv-grid__skeleton {
  background: linear-gradient(
    90deg,
    var(--lv-grid-gray-100) 25%,
    var(--lv-grid-gray-200) 50%,
    var(--lv-grid-gray-100) 75%
  );
  background-size: 200% 100%;
  animation: lv-grid-skeleton-pulse 1.5s infinite;
}

/* 행 선택 펄스 */
@keyframes lv-grid-select-pulse {
  0% { background: var(--lv-grid-selected); }
  50% { background: var(--lv-grid-primary-light); }
  100% { background: var(--lv-grid-selected); }
}

.lv-grid__row--selected {
  animation: lv-grid-select-pulse 0.3s ease-out;
}
```

---

## 유틸리티 클래스

### 텍스트 정렬

```css
.lv-grid-text-left { text-align: left; }
.lv-grid-text-center { text-align: center; }
.lv-grid-text-right { text-align: right; }
```

### 텍스트 오버플로

```css
.lv-grid-truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

### 표시/숨김

```css
.lv-grid-hide-mobile {
  @media (max-width: 639px) {
    display: none !important;
  }
}

.lv-grid-hide-tablet {
  @media (min-width: 640px) and (max-width: 1023px) {
    display: none !important;
  }
}

.lv-grid-hide-desktop {
  @media (min-width: 1024px) {
    display: none !important;
  }
}
```

---

## 완전한 CSS 예시

```css
/* ========================================
   LiveView Grid Stylesheet v0.1.0
   ======================================== */

/* 1. Variables */
:root {
  /* Colors */
  --lv-grid-primary: #2196f3;
  --lv-grid-bg: #ffffff;
  --lv-grid-text: #333333;
  --lv-grid-border: #e0e0e0;
  --lv-grid-hover: #f5f5f5;
  --lv-grid-selected: #e3f2fd;
  
  /* Spacing */
  --lv-grid-space-2: 8px;
  --lv-grid-space-3: 12px;
  --lv-grid-space-4: 16px;
  
  /* Typography */
  --lv-grid-font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --lv-grid-font-size-md: 14px;
  
  /* Transitions */
  --lv-grid-transition-normal: 0.2s ease;
}

/* 2. Reset */
.lv-grid,
.lv-grid * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 3. Container */
.lv-grid {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  
  background: var(--lv-grid-bg);
  border: 1px solid var(--lv-grid-border);
  border-radius: 4px;
  overflow: hidden;
  
  font-family: var(--lv-grid-font-family);
  font-size: var(--lv-grid-font-size-md);
  color: var(--lv-grid-text);
}

/* 4. Header */
.lv-grid__header {
  display: flex;
  background: #fafafa;
  border-bottom: 2px solid var(--lv-grid-border);
  position: sticky;
  top: 0;
  z-index: 10;
}

.lv-grid__header-cell {
  padding: var(--lv-grid-space-3) var(--lv-grid-space-4);
  height: 48px;
  display: flex;
  align-items: center;
  font-weight: 600;
  user-select: none;
}

.lv-grid__header-cell--sortable {
  cursor: pointer;
  transition: background var(--lv-grid-transition-normal);
}

.lv-grid__header-cell--sortable:hover {
  background: #eeeeee;
}

/* 5. Body */
.lv-grid__body {
  overflow-y: auto;
  max-height: 600px;
}

.lv-grid__row {
  display: flex;
  border-bottom: 1px solid #f0f0f0;
  transition: background var(--lv-grid-transition-normal);
}

.lv-grid__row:hover {
  background: var(--lv-grid-hover);
}

.lv-grid__row--selected {
  background: var(--lv-grid-selected);
  border-left: 3px solid var(--lv-grid-primary);
}

.lv-grid__cell {
  padding: var(--lv-grid-space-2) var(--lv-grid-space-4);
  height: 40px;
  display: flex;
  align-items: center;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 6. Footer */
.lv-grid__footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--lv-grid-space-3) var(--lv-grid-space-4);
  background: #fafafa;
  border-top: 1px solid var(--lv-grid-border);
}

.lv-grid__pagination {
  display: flex;
  gap: 4px;
}

.lv-grid__page-btn {
  min-width: 36px;
  height: 36px;
  padding: 0 12px;
  border: 1px solid var(--lv-grid-border);
  border-radius: 4px;
  background: white;
  cursor: pointer;
  transition: all var(--lv-grid-transition-normal);
}

.lv-grid__page-btn:hover {
  background: var(--lv-grid-hover);
  border-color: var(--lv-grid-primary);
}

.lv-grid__page-btn--current {
  background: var(--lv-grid-primary);
  color: white;
  border-color: var(--lv-grid-primary);
}

/* 7. Dark Mode */
[data-theme="dark"] .lv-grid {
  --lv-grid-bg: #1e1e1e;
  --lv-grid-text: #e0e0e0;
  --lv-grid-border: #3a3a3a;
  --lv-grid-hover: #2a2a2a;
  --lv-grid-selected: #1a3a5a;
}
```

---

## 버전 이력

| 버전 | 날짜 | 변경사항 |
|------|------|----------|
| v0.1.0 | 2026-02-20 | 초안 작성, BEM 규칙 정의 |

---

## 다음 단계

- [ ] CSS 파일 작성 (`assets/css/liveview_grid.css`)
- [ ] 다크 모드 테스트
- [ ] 반응형 디자인 검증
- [ ] 접근성 (WCAG 2.1 AA) 준수 확인

🐾
