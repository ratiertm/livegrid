# 훅(Hook) 설정 가이드

> Claude Code의 hooks 기능으로 자동 매뉴얼 읽기와 품질 검사를 설정합니다.

## hooks 기능을 사용할 수 없는 경우 — CLAUDE.md에 직접 명시

CLAUDE.md에 아래 규칙을 추가하세요:

```markdown
## 🔁 자동 실행 규칙 (매 작업마다 반드시 따를 것)

### 작업 시작 전 (PreTask)
1. LiveView/컴포넌트/HEEx 관련 키워드 → `.claude/skills/liveview/INDEX.md` 읽기
2. Context/Ecto/스키마/쿼리 관련 키워드 → `.claude/skills/backend/INDEX.md` 읽기
3. `.claude/tasks/current/` 에 진행 중인 작업이 있으면 → 계획서 + 체크리스트 먼저 읽기

### 작업 완료 후 (PostTask)
1. 수정한 파일 목록 정리
2. 컴파일 체크: `mix compile --warnings-as-errors`
3. 포맷 체크: `mix format --check-formatted`
4. 수정 관련 테스트 실행: `mix test [관련 파일]`
5. 셀프 체크 리마인더:
   - Changeset 유효성 검증은 빠짐없는가?
   - Context를 통하지 않고 Repo를 직접 호출한 곳은 없는가?
   - 에러 처리는 {:ok, _} / {:error, _} 패턴을 따르는가?
   - @spec, @doc은 공개 함수에 작성했는가?
6. 체크리스트 파일 업데이트
```

## 품질 검사 스크립트 (수동 실행용)

```bash
#!/bin/bash
# .claude/hooks/quality-check.sh
echo "🔍 ContentFlow 품질 검사 실행 중..."

echo "--- 컴파일 체크 ---"
mix compile --warnings-as-errors 2>&1 | tail -20

echo "--- 포맷 체크 ---"
mix format --check-formatted 2>&1

echo "--- 테스트 실행 ---"
mix test 2>&1 | tail -30

echo "--- Credo 정적 분석 ---"
mix credo --strict 2>&1 | tail -20

echo "✅ 검사 완료"
```
